<Window x:Class="FlagDocUploader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlagDocUploader"
        mc:Ignorable="d"
        Title="ZIP Folder Uploader - Circular Flag" 
        Height="750" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#154876"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#154876" 
                Padding="20"
                Margin="-20,-20,-20,10"
                CornerRadius="0">
            <StackPanel>
                <TextBlock Text="Circular Flag Document Upload Utility" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"/>
                <TextBlock Text="Upload Circular Flag documents (ZIP)" 
                          FontSize="14" 
                          Foreground="White" 
                          Opacity="0.9"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Connection Status -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#DDDDDD" 
                BorderThickness="1"
                Padding="15"
                Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Database Status: " FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ConnectionStatus}" 
                          Foreground="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"/>
                <Button Content="Test Connection" 
                        Command="{Binding TestConnectionCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="10,5"
                        Margin="15,0,0,0"/>
            </StackPanel>
        </Border>

        
  
        

        <!-- Configuration -->
        <GroupBox Grid.Row="2" Header="Configuration">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Folder Selection -->
                <GroupBox Grid.Row="0" Grid.Column="0" Header="Target Folder Selection">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Label Grid.Row="0" Content="Select Folder (Circular Flag):"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0"
  ItemsSource="{Binding AvailableFolders}"
  SelectedItem="{Binding SelectedFolder, Mode=TwoWay}"
  DisplayMemberPath="FolderName"
  IsEnabled="{Binding IsLoadingFolders, Converter={StaticResource InverseBoolConverter}}"
  VerticalContentAlignment="Center">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="Padding" Value="8"/>
                                        <Setter Property="Margin" Value="0,0,0,10"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AvailableFolders.Count}" Value="0">
                                                <Setter Property="Background" Value="#FFF3E0"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ComboBox.Style>
                            </ComboBox>

                            <Button   Grid.Column="1"

Command="{Binding RefreshFoldersCommand}"
Style="{StaticResource SecondaryButton}"
Width="100"
IsEnabled="{Binding IsLoadingFolders, Converter={StaticResource InverseBoolConverter}}">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔄" Margin="0,0,5,0"/>
                                        <TextBlock Text="Refresh"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>

                        </Grid>
                        <!--Loading indicator-->
                        <StackPanel Grid.Row="1"  
Orientation="Horizontal" 
Margin="0,5,0,0"
Visibility="{Binding IsLoadingFolders, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="⏳ Loading folders..." 
   FontStyle="Italic" 
   Foreground="#666666"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>
          
                
                <!-- File Selection -->
                <GroupBox Grid.Row="0"  Grid.Column="1" Header="File Selection">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" 
                Text="{Binding SelectedFilePath, Mode=TwoWay}" 
                IsReadOnly="True"
                VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" 
               Content="Browse..." 
               Command="{Binding BrowseCommand}"
               Style="{StaticResource SecondaryButton}"
               Width="120"/>
                    </Grid>
                </GroupBox>

                <Label Grid.Row="1" Grid.Column="0" Content="Workspace ID:"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                        Text="{Binding WorkspaceId, Mode=TwoWay}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Category ID:"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                        Text="{Binding CategoryId, Mode=TwoWay}"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Category Name:"/>
                <TextBox Grid.Row="3" Grid.Column="1" 
                        Text="{Binding CategoryName, Mode=TwoWay}"/>

                <Label Grid.Row="4" Grid.Column="0" Content="User ID:"/>
                <TextBox Grid.Row="4" Grid.Column="1" 
                        Text="{Binding UserId, Mode=TwoWay}"/>
            </Grid>
        </GroupBox>
        <!-- Note Section -->
        <GroupBox Grid.Row="3" Header="Note">
            <StackPanel>
                <TextBlock Text="When a zip file is uploaded, the previous folder, subfolders, and documents for that specific zip will be archived , and the latest upload will be shown as active." 
              FontSize="14" 
              FontWeight="Bold" 
              Foreground="#F44336"/>
            </StackPanel>
        </GroupBox>
        <!-- Progress Section -->
        <GroupBox Grid.Row="4" Header="Progress">
            <StackPanel>
                <TextBlock Text="{Binding CurrentOperation}" 
                          FontSize="14" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"/>

                <ProgressBar Height="30" 
                            Value="{Binding ProgressPercentage}" 
                            Maximum="100"
                            Margin="0,0,0,10"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock>
                            <Run Text="Folders: "/>
                            <Run Text="{Binding ProcessedFolders}" FontWeight="Bold"/>
                            <Run Text=" / "/>
                            <Run Text="{Binding TotalFolders}" FontWeight="Bold"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock>
                            <Run Text="Files: "/>
                            <Run Text="{Binding ProcessedFiles}" FontWeight="Bold"/>
                            <Run Text=" / "/>
                            <Run Text="{Binding TotalFiles}" FontWeight="Bold"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <Border Background="#FFF9C4" 
                       BorderBrush="#FBC02D" 
                       BorderThickness="1"
                       Padding="10"
                       Margin="0,15,0,0"
                       Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock Text="{Binding StatusMessage}" 
                              TextWrapping="Wrap"
                              FontSize="12"/>
                </Border>
            </StackPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right"
                   Margin="0,10,0,0">
            <Button Content="Process ZIP File" 
                   Command="{Binding ProcessCommand}"
                   Style="{StaticResource PrimaryButton}"
                   Width="150"/>
            <Button Content="Cancel" 
                   Command="{Binding CancelCommand}"
                   Style="{StaticResource DangerButton}"
                   Width="100"/>
        </StackPanel>
    </Grid>
</Window>
